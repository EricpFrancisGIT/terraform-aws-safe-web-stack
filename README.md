# terraform-aws-safe-web-stack

## Overview

This project demonstrates Terraform Associate 004 concepts including safe workflows, state management, modular design, and team-ready infrastructure practices.

## Day 1 — Bootstrap

- Initialized Terraform project
- Configured AWS provider using variables
- Created one S3 bucket as the first managed resource
- Verified workflow using:
  - `terraform fmt`
  - `terraform validate`
  - `terraform init`
  - `terraform plan`
  - `terraform apply`

## Terraform Concepts Demonstrated

- Provider configuration and version constraints
- Variables and tfvars usage
- Outputs for resource identification
- Safe Terraform workflow
- Infrastructure verification in AWS Console

## Usage

```bash
terraform fmt
terraform validate
terraform init
terraform plan
terraform apply

Author
Eric Paul Michael Francis


👉 Save, commit, and push:

```powershell
git add README.md
git commit -m "Update README with Day 1 summary"
git push
